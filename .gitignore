# ── 会话数据（运行时生成，不入库）──────────────────────────
agent-sessions.json
agent-v2-sessions.json

# ── 临时 / 备份文件 ────────────────────────────────────────
*.bak
*.tmp
nul

# ── 压缩包 ─────────────────────────────────────────────────
proxy.zip

# ── 本地开发辅助脚本（含敏感路径） ─────────────────────────
start-all.bat
start-proxy.ps1
deploy-cf.ps1

# ── 旧版 Agent（已废弃，保留在本地不入库） ──────────────────
agent/
agent-scf-download/

# ── Node 依赖（各子目录的 node_modules） ────────────────────
node_modules/
**/node_modules/

# ── 构建产物 ────────────────────────────────────────────────
dist/
.cache/

# ── 操作系统 ────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Vercel（含 project ID，不入库） ─────────────────────────
.vercel/

# ── 编辑器 ──────────────────────────────────────────────────
.vscode/
.idea/

# ── 环境变量（如有本地 .env） ────────────────────────────────
.env
.env.local
.env.*.local
