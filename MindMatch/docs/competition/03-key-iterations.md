# 三、关键迭代过程

**迭代一：匹配算法**

- 迭代前：阶跃函数硬截断，存在决策死区；单一算法路径，无交叉验证
- 迭代后：高斯核（σ=0.20）连续可导消除死区；TOPSIS 双轨混合交叉验证；两级匹配（6 方向 → 54 职业）

**迭代二：Agent 架构**

- 迭代前：v1 五阶段独立对话，状态分散，工具全集常驻，易出现跳阶和幻觉
- 迭代后：v2 四阶段工具编排，统一状态机，动态工具枚举（每阶段仅暴露允许工具），加入 `ensureEndsWithQuestion` 后处理器强制提问
